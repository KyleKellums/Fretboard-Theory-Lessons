@model IEnumerable<Fretboard_Theory_Course.Models.Resources>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ResourceType)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResourceType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ResourcesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ResourcesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ResourcesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
